from setuptools import setup, find_packages


setup(
      name        = 'YotsuBar',
      version     = '1.0.0',
      description = 'Python progress bar',
      author      = 'Yotsuba',
      license     = 'MIT',
      packages    = find_packages(),
      )
