from setuptools import setup, find_packages


setup(
      name        = 'YotsuBar',
      version     = '1.0.0',
      description = 'A progress bar module in Python',
      author      = 'Yotsuba',
      license     = 'MIT',
      packages    = find_packages(),
      )
